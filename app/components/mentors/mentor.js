import React, { Component } from 'react';
import { StyleSheet, Text, View, FlatList, ActivityIndicator } from 'react-native';

export default class Mentor extends Component {
    render(){
        return(
            <Text>The Singular Mentor</Text>
        )
    }
}